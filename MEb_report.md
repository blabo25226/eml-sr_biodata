このファイルはeml-sr_biodata/Manuscript_EML_biophysics/(以下ではMEbとする。)とその論文 @2605.02972v1.pdf をまとめたファイルである。

# 1.論文の概要
タイトル： **Non-Monotone Response Modules and Cascades from the EML Operator for Reduced Models of Biological Dynamics**
著者： Amir Erez

生物の動態モデルにおいて広く使われる「ヒル関数」などの応答関数は、単調（monotone）な性質を持つため、単一の数式ブロックでは「オーバーシュート（一時的に上昇した後に下降する現象）」や「適応的な過渡応答」のような非単調（non-monotone）な反応を表現できません。従来の関数で非単調な反応をモデリングするには、複数のブロックを組み合わせる必要があり、パラメータ数が倍増してしまいます。

本論文では、単一の二項演算子である **EML（Elementary Mathematical Logic）** を用いることで、あらゆる初等関数を生成できるという数学的な性質を利用し、生物学的なダイナミクスを表現するための簡略化された非線形常微分方程式（ODE）の「構造化された文法」としてEMLを提案しています。
これにより、たった1つのブロックでオーバーシュートなどを直接表現できる「活性化-抑制（activation-suppression）」モジュールを構築することが可能になります。

論文内では、以下の3つの検証を通じてEMLフレームワークの有効性を示しています：
1. **PKA-R再局在化データ (LaCroix et al.)**: EML文法を用いた探索により、既知の複雑なメカニズムモデルと一致する、シンプルで正確な代替モデルを発見しました。
2. **Rho-GTPaseの摂動応答データ (Nanda et al.)**: EML数式ツリーを網羅的に探索した結果、4つの異なる応答データすべてにおいて共通する最適な数式構造（モジュール）が選択され、ヒル関数よりも少ないパラメータで同等以上の表現力を持つことを示しました。
3. **50状態の合成ネットワークの粗視化**: 大規模（50状態）なシミュレーションネットワークに対し、EMLカスケードを固定の「時間的基底関数」として使用することで、非常に低次元のモデルへと圧縮（粗視化）できることを実証しました。

# 2.MEbの概要
現在、`eml-sr_biodata/Manuscript_EML_biophysics/` フォルダ内は `lacroix/`、`nanda/`、`toy/` の3つのサブフォルダに整理されており、それぞれの検証に対応するスクリプトやデータが格納されています。

各主要ファイルの内容は以下の通りです：

### ドキュメントと実行スクリプト
* `README.md`: リポジトリの概要、要件、データセットの出典、およびコードの実行方法が記載されています。
* `runme.sh`: 論文のFig.1〜Fig.4を生成するためのPythonスクリプト群を順番に実行するシェルスクリプトです。

### データセット
* `lacroix/LaCroix-elife-66869-fig2-data.csv`: ラパマイシンによるPKA活性の時系列データ（LaCroixらの論文より）。
* `nanda/Nanda-Fig2d.csv`: Rho-GTPaseの摂動とそれに対する応答の時系列データ（Nandaらの論文より）。

### 解析用Pythonスクリプト
* `lacroix/lacroix_grammar_search_ode.py`: LaCroixのデータに対してEML文法探索を行い、一次緩和常微分方程式(ODE)に当てはめるスクリプト。論文の **Fig. 1** を生成します。
* `eml_grammar_search.py`: Nandaの4つのRho-GTPase応答データに対し、EML文法を用いた「制約付きシンボリック回帰（symbolic-regression）探索」を行う中心的なスクリプトです。木構造の数式（例: `E ::= R | G(E) | E + E`）を列挙・フィッティングし、wMSEやAIC/BICでモデルを評価します。論文の **Fig. 2** を生成します。
* `hill_grammar_search.py`: 比較検証として、Nandaのデータに対して従来のヒル関数を用いた探索を行うスクリプト。論文の **Fig. 3** を生成します。
* `toy/toy_coarse_graining_benchmark.py`: 50状態の複雑な活性化-適応ネットワークをシミュレートし、それをEMLカスケードによって低次元に圧縮（粗視化）できるかを検証するスクリプト。論文の **Fig. 4** を生成します。

*(※ 上記のスクリプトを実行することで、対応するPDF/SVG形式のグラフ画像や、AIC/BICの比較結果をまとめたCSVファイルがディレクトリ内に出力されます。)*

# 3.略語・用語一覧
本ドキュメントおよび関連ファイル内で使用されている主な略語・用語の解説です。

* **MEb**: `Manuscript_EML_biophysics` フォルダの略称。本ドキュメント内での呼称であり、当該論文のコードやデータを格納しているリポジトリのディレクトリを指します。
* **ODE**: Ordinary Differential Equation（常微分方程式）の略。時間とともに変化する変数の関係を記述する方程式で、本論文では生体分子の反応ダイナミクスをモデル化するために使用されています。
* **eml (EML)**: Elementary Mathematical Logic の略。本論文で焦点となっている特殊な二項演算子（EML operator）のことです。この演算子を利用した「EML文法」を用いることで、複雑な生物学的ダイナミクスをシンプルな数式でモデル化しています。
* **all4**: Nandaのデータセットに含まれる「4つの応答データすべて（all 4 traces）」を意味します。ファイル名（例：`nanda_grammar_all4_summary.csv`）などで、4つの実験条件すべてに対する解析結果が含まれていることを示します。
* **nanda (Nanda)**: 論文内で検証用データとして引用されている Nanda et al. (2023, Nature Communications) の研究を指します。具体的には、細胞膜におけるRho-GTPase（RhoAおよびRac1）の動態（摂動を与えた際の反応）を測定した4つの時系列データセットのことです。このデータは、非単調な反応（オーバーシュートなど）を含むため、EMLモデルと既存のヒル関数モデルの表現力を比較するための重要なテストケースとして使用されています。
* **PKA-R**: プロテインキナーゼA（PKA）の調節サブユニット（Regulatory subunit）のことです。本論文のLaCroixのデータセットでは、ラパマイシンという薬剤を用いてこのPKA-Rを細胞膜へ強制的に移動（再局在化）させており、その際のPKA活性の複雑な変化をEMLでモデル化しています。
* **Rho-GTPase**: 細胞骨格の制御や細胞運動などを司る低分子量GTP結合タンパク質群（RhoAやRac1など）のことです。本論文のNandaのデータセットにおいて、これらに摂動（人工的な活性化など）を与えた際の動態の変化が分析対象となっています。
* **lacroix (LaCroix)**: 論文内で検証用データとして引用されている LaCroix et al. (2022, eLife) の研究を指します。ラパマイシンによって誘導されるPKA（プロテインキナーゼA）の調節サブユニットの再局在化に関する実験データのことで、既存のメカニズムが判明している系において、EMLが適切な代替モデルを発見できるかを検証するために使用されています。
* **toy**: 「トイモデル（toy model）」や「トイベンチマーク」のことです。現実の複雑なシステムを模して作られた、単純化された理論上の人工モデルを指します。本論文では、50状態の複雑な活性化-適応ネットワークを人工的にシミュレートした合成データ（toy benchmark）を作成し、EMLの有効性をテストしています。
* **runme**: リポジトリに含まれる実行用のシェルスクリプト `runme.sh` のことです。"Run me（私を実行して）" という意味の通り、このスクリプトを実行するだけで論文のすべての図（Fig. 1〜4）や解析結果を全自動で再現・出力できるようになっています。
* **coarse**: 「粗視化（coarse-graining）」のことです。複雑で高次元のシステム（多数の変数や状態を持つモデル）の微視的な詳細を省き、システム全体としての重要な巨視的な振る舞いだけを残して、より単純な低次元のシステムに圧縮・近似する手法を指します。本論文の後半では、EMLカスケードを用いて複雑なネットワークを効果的に「粗視化」できるかを検証しています。
